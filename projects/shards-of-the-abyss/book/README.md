# Редакторская копия (сентябрь 2026)

Полный снимок рабочей ветки `arena/01a0617f-shardsoftheabyss` на момент закрытия главы 16 (коммит `01dca8e`).
Полная история правок — в самой ветке; здесь — плоская копия для чтения из `main`.

- `03-manuscript/` — рукопись: главы 1–16 отредактированы, 17–24 нетронуты (база).
- `04-review/ch-NN/` — по каждой главе: пакет разбора (`ПАКЕТ-*.md`), заметки чтения (`reread/`), скрипты правок (`edits_*.py`), diff, таблица «было → стало» (`МЕЛКИЕ-было-стало.md`), журнал (`EDIT-LOG-*.md`), копия до правок (`*.before-*.md`).
- `05-publication/Глава_NN.pdf` — PDF глав 1–16 после правок; `author-pdf-2026-09-01/` — авторские PDF от 01.09 и их md.
- `01-canon/` — библия проекта и `FACTS.tsv` (факты по главам, в т.ч. хронология и долги).
- `02-writing-system/` — система голоса/стиля; `scripts/` — tics/nums/dialog_shape/build_pdf/pairs_table.
- `90-archive/` — всё, что убрано при чистке репозитория (ничего не удалено без архива).
